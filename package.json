{
  "name": "mortgage-calculator",
  "version": "1.0.0",
  "description": "Simple mortgage calculator for comparing interest rates, hosted on GitHub Pages.",
  "type": "module",
  "scripts": {
    "dev": "node build.mjs --watch --serve",
    "build": "node build.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
